cipher:
  aes:
    cbc:
      # 32-byte (256-bit) AES key, Base64-encoded — override via env vars in production
      key: ${AES_KEY:c2VjcmV0S2V5Rm9yQWVzMjU2Qml0c0tleVZhbHVlISE=}
      # 16-byte IV, Base64-encoded
      iv: ${AES_IV:aW5pdFZlY3RvcjE2Qnl0ZQ==}

